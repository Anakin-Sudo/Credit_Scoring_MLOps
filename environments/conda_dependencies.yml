name: credit-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - scikit-learn
      - imblearn
      - category_encoders
      - joblib
      - python-dotenv
      - xgboost
      - mlflow
