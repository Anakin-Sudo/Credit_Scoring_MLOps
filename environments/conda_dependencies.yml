name: credit-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - azure-ai-ml
      - azure-identity
      - scikit-learn
      - xgboost
      - pandas
      - joblib
      - mlflow
